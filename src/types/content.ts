export type ContentType = {
  description: string;
  docId: string;
  genre: string;
  id: string;
  maturity: string;
  slug: string;
  title: string;
};

export type InitialContentType = {
  docId: string;
};
